let create = require('./create');
const expect = require('chai').expect;

describe('create', () => {

    it('should test that product is inputted into db', () => {
        let myObj = document.getElementById("submit");
        myObj.on('onclick', function() {
            assert()
        })
    
    })
})